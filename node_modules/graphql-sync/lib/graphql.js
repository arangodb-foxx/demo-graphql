
/**
 *  Copyright (c) 2015, Facebook, Inc.
 *  All rights reserved.
 *
 *  This source code is licensed under the BSD-style license found in the
 *  LICENSE file in the root directory of this source tree. An additional grant
 *  of patent rights can be found in the PATENTS file in the same directory.
 */

/**
 * This is the primary entry point function for fulfilling GraphQL operations
 * by parsing, validating, and executing a GraphQL document along side a
 * GraphQL schema.
 *
 * More sophisticated GraphQL servers, such as those which persist queries,
 * may wish to separate the validation and execution phases to a static time
 * tooling step, and a server runtime step.
 */
'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports.graphql = graphql;

/**
 * The result of a GraphQL parse, validation and execution.
 *
 * `data` is the result of a successful execution of the query.
 * `errors` is included when any errors occurred as a non-empty array.
 */

var _graphqlLanguageSource = require('graphql/language/source');

var _graphqlLanguageParser = require('graphql/language/parser');

var _graphqlValidationValidate = require('graphql/validation/validate');

var _executionExecute = require('./execution/execute');

function graphql(schema, requestString, rootValue, variableValues, operationName) {
  try {
    var source = new _graphqlLanguageSource.Source(requestString || '', 'GraphQL request');
    var documentAST = (0, _graphqlLanguageParser.parse)(source);
    var validationErrors = (0, _graphqlValidationValidate.validate)(schema, documentAST);
    if (validationErrors.length > 0) {
      return { errors: validationErrors };
    }
    return (0, _executionExecute.execute)(schema, documentAST, rootValue, variableValues, operationName);
  } catch (error) {
    return { errors: [error] };
  }
}